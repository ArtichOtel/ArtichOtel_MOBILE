export type userDataType = {
    userId: string | null,
    token: string | null
    customerId: string | null
}

// TODO : change any type
export type criteriaType = {
    arrivalDate: any,
    departureDate: any,
    roomTypes: string,
    peopleNbr: number,
}