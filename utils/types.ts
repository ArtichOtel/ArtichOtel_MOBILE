export type userDataType = {
    userId: string | null,
    token: string | null
    customerId: string | null
}

// TODO : change any type
export type criteriaType = {
    startDate: any,
    endDate: any,
    roomTypes: string,
    peopleNbr: number,
}