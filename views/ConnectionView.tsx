import {useState} from "react";
import {View} from "react-native";

type ConnectionProps = {
    placeholder: string;
}


export function ConnectionView(props: ConnectionProps): JSX.Element {
    const {placeholder} = props

    return (
        <View style={}
    )
}